{% extends 'base.html.twig' %}

{% block title %}Edit Asset
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row">
			<div class="col-12 text-center my-4">
				<h4 class="center">Add New Place</h4>
			</div>
		</div>
		{{ include('asset/_form.html.twig', {'button_label': 'Update'}) }}

		<div class="row">
			<div class="col-12 text-center my-4">
				<a class="btn btn-block btn-success" href="{{ path('app_asset_index') }}">back to list</a>
				{{ include('asset/_delete_form.html.twig') }}
			</div>
		</div>

	</div>

{% endblock %}
