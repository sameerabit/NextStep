<form method="post" action="{{ path('app_phone_directory_delete', {'id': phone_directory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ phone_directory.id) }}">
    <button class="btn">Delete</button>
</form>
