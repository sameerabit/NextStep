{% extends 'base.html.twig' %}

{% block title %}PhoneDirectory{% endblock %}

{% block body %}
    <h1>PhoneDirectory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phone_directory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ phone_directory.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ phone_directory.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_phone_directory_index') }}">back to list</a>

    <a href="{{ path('app_phone_directory_edit', {'id': phone_directory.id}) }}">edit</a>

    {{ include('phone_directory/_delete_form.html.twig') }}
{% endblock %}
